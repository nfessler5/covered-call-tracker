// Code generated by goa, DO NOT EDIT.
//
// positions service
//
// Command:
// $ goa gen covered-call-tracker/design

package positions

import (
	"context"
)

// Service for managing covered call positions.
type Service interface {
	// List active or historical covered call positions.
	List(context.Context, *ListPayload) (res []*CoveredCallPosition, err error)
	// Open a new covered call position.
	Create(context.Context, *CoveredCallPosition) (res *CoveredCallPosition, err error)
	// Close an existing position as ROLLED and create a new open position in one
	// atomic transaction.
	Roll(context.Context, *RollPayload) (res *CoveredCallPosition, err error)
}

// APIName is the name of the API as defined in the design.
const APIName = "covered_call_tracker"

// APIVersion is the version of the API as defined in the design.
const APIVersion = "0.0.1"

// ServiceName is the name of the service as defined in the design. This is the
// same value that is set in the endpoint request contexts under the ServiceKey
// key.
const ServiceName = "positions"

// MethodNames lists the service method names as defined in the design. These
// are the same values that are set in the endpoint request contexts under the
// MethodKey key.
var MethodNames = [3]string{"list", "create", "roll"}

// CoveredCallPosition is the payload type of the positions service create
// method.
type CoveredCallPosition struct {
	// Unique Position ID
	ID *int
	// ID of the underlying stock
	UnderlyingID int
	// Option Strike Price
	StrikePrice float64
	// Expiration Date (YYYY-MM-DD)
	ExpirationDate string
	// Total premium collected for the contracts
	PremiumCollected float64
	// Number of contracts sold
	ContractsCount int
	// Status: OPEN, EXPIRED, ASSIGNED, ROLLED, CLOSED
	Status string
}

// ListPayload is the payload type of the positions service list method.
type ListPayload struct {
	// Filter by status (OPEN, EXPIRED, ASSIGNED, ROLLED, CLOSED)
	Status *string
}

// RollPayload is the payload type of the positions service roll method.
type RollPayload struct {
	// ID of the position being rolled
	ID int
	// New contract details
	RollDetails *RollPositionPayload
}

// Parameters required to roll an existing position into a new expiration and
// strike.
type RollPositionPayload struct {
	// New strike price
	NewStrikePrice float64
	// New expiration date (YYYY-MM-DD)
	NewExpirationDate string
	// Net credit collected (or debit paid) during the roll
	NetCredit float64
}
