// Code generated by goa, DO NOT EDIT.
//
// tracker HTTP client CLI support package
//
// Command:
// $ goa gen covered-call-tracker/design

package cli

import (
	positionsc "covered-call-tracker/gen/http/positions/client"
	scannerc "covered-call-tracker/gen/http/scanner/client"
	usersc "covered-call-tracker/gen/http/users/client"
	"flag"
	"fmt"
	"net/http"
	"os"

	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"users get",
		"scanner scan",
		"positions (list|create|roll)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "users get --id 2275113624697443502" + "\n" +
		os.Args[0] + " " + "scanner scan --body '{\n      \"min_annualized_yield\": 0.826306896283667,\n      \"tickers\": [\n         \"Rerum sit placeat est delectus assumenda.\",\n         \"Dolorem ut dolorem perspiciatis.\",\n         \"Quaerat perspiciatis quia ipsum tempore alias.\",\n         \"Excepturi iusto minus.\"\n      ]\n   }'" + "\n" +
		os.Args[0] + " " + "positions list --status \"Aut corrupti magnam.\"" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		usersFlags = flag.NewFlagSet("users", flag.ContinueOnError)

		usersGetFlags  = flag.NewFlagSet("get", flag.ExitOnError)
		usersGetIDFlag = usersGetFlags.String("id", "REQUIRED", "User ID")

		scannerFlags = flag.NewFlagSet("scanner", flag.ContinueOnError)

		scannerScanFlags    = flag.NewFlagSet("scan", flag.ExitOnError)
		scannerScanBodyFlag = scannerScanFlags.String("body", "REQUIRED", "")

		positionsFlags = flag.NewFlagSet("positions", flag.ContinueOnError)

		positionsListFlags      = flag.NewFlagSet("list", flag.ExitOnError)
		positionsListStatusFlag = positionsListFlags.String("status", "", "")

		positionsCreateFlags    = flag.NewFlagSet("create", flag.ExitOnError)
		positionsCreateBodyFlag = positionsCreateFlags.String("body", "REQUIRED", "")

		positionsRollFlags    = flag.NewFlagSet("roll", flag.ExitOnError)
		positionsRollBodyFlag = positionsRollFlags.String("body", "REQUIRED", "")
		positionsRollIDFlag   = positionsRollFlags.String("id", "REQUIRED", "ID of the position being rolled")
	)
	usersFlags.Usage = usersUsage
	usersGetFlags.Usage = usersGetUsage

	scannerFlags.Usage = scannerUsage
	scannerScanFlags.Usage = scannerScanUsage

	positionsFlags.Usage = positionsUsage
	positionsListFlags.Usage = positionsListUsage
	positionsCreateFlags.Usage = positionsCreateUsage
	positionsRollFlags.Usage = positionsRollUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "users":
			svcf = usersFlags
		case "scanner":
			svcf = scannerFlags
		case "positions":
			svcf = positionsFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "users":
			switch epn {
			case "get":
				epf = usersGetFlags

			}

		case "scanner":
			switch epn {
			case "scan":
				epf = scannerScanFlags

			}

		case "positions":
			switch epn {
			case "list":
				epf = positionsListFlags

			case "create":
				epf = positionsCreateFlags

			case "roll":
				epf = positionsRollFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "users":
			c := usersc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "get":
				endpoint = c.Get()
				data, err = usersc.BuildGetPayload(*usersGetIDFlag)
			}
		case "scanner":
			c := scannerc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "scan":
				endpoint = c.Scan()
				data, err = scannerc.BuildScanPayload(*scannerScanBodyFlag)
			}
		case "positions":
			c := positionsc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
				data, err = positionsc.BuildListPayload(*positionsListStatusFlag)
			case "create":
				endpoint = c.Create()
				data, err = positionsc.BuildCreatePayload(*positionsCreateBodyFlag)
			case "roll":
				endpoint = c.Roll()
				data, err = positionsc.BuildRollPayload(*positionsRollBodyFlag, *positionsRollIDFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// usersUsage displays the usage of the users command and its subcommands.
func usersUsage() {
	fmt.Fprintln(os.Stderr, `Service for managing application users.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] users COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    get: Get user profile by ID.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s users COMMAND --help\n", os.Args[0])
}
func usersGetUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] users get", os.Args[0])
	fmt.Fprint(os.Stderr, " -id INT64")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get user profile by ID.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id INT64: User ID`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "users get --id 2275113624697443502")
}

// scannerUsage displays the usage of the scanner command and its subcommands.
func scannerUsage() {
	fmt.Fprintln(os.Stderr, `In-app scanning engine service`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] scanner COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    scan: Scan implements scan.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s scanner COMMAND --help\n", os.Args[0])
}
func scannerScanUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] scanner scan", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Scan implements scan.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "scanner scan --body '{\n      \"min_annualized_yield\": 0.826306896283667,\n      \"tickers\": [\n         \"Rerum sit placeat est delectus assumenda.\",\n         \"Dolorem ut dolorem perspiciatis.\",\n         \"Quaerat perspiciatis quia ipsum tempore alias.\",\n         \"Excepturi iusto minus.\"\n      ]\n   }'")
}

// positionsUsage displays the usage of the positions command and its
// subcommands.
func positionsUsage() {
	fmt.Fprintln(os.Stderr, `Service for managing covered call positions.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] positions COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    list: List active or historical covered call positions.`)
	fmt.Fprintln(os.Stderr, `    create: Open a new covered call position.`)
	fmt.Fprintln(os.Stderr, `    roll: Close an existing position as ROLLED and create a new open position in one atomic transaction.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s positions COMMAND --help\n", os.Args[0])
}
func positionsListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] positions list", os.Args[0])
	fmt.Fprint(os.Stderr, " -status STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List active or historical covered call positions.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -status STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "positions list --status \"Aut corrupti magnam.\"")
}

func positionsCreateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] positions create", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Open a new covered call position.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "positions create --body '{\n      \"contracts_count\": 1,\n      \"expiration_date\": \"2026-09-18\",\n      \"id\": 10,\n      \"premium_collected\": 150,\n      \"status\": \"OPEN\",\n      \"strike_price\": 30,\n      \"underlying_id\": 1\n   }'")
}

func positionsRollUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] positions roll", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -id INT")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Close an existing position as ROLLED and create a new open position in one atomic transaction.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -id INT: ID of the position being rolled`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "positions roll --body '{\n      \"roll_details\": {\n         \"net_credit\": 45,\n         \"new_expiration_date\": \"2026-09-25\",\n         \"new_strike_price\": 32\n      }\n   }' --id 3190368447098624533")
}
