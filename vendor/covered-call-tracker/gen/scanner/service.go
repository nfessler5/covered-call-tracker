// Code generated by goa, DO NOT EDIT.
//
// scanner service
//
// Command:
// $ goa gen covered-call-tracker/design

package scanner

import (
	"context"
)

// In-app scanning engine service
type Service interface {
	// Scan implements scan.
	Scan(context.Context, *ScanPayload) (res []*Candidate, err error)
}

// APIName is the name of the API as defined in the design.
const APIName = "covered_call_tracker"

// APIVersion is the version of the API as defined in the design.
const APIVersion = "0.0.1"

// ServiceName is the name of the service as defined in the design. This is the
// same value that is set in the endpoint request contexts under the ServiceKey
// key.
const ServiceName = "scanner"

// MethodNames lists the service method names as defined in the design. These
// are the same values that are set in the endpoint request contexts under the
// MethodKey key.
var MethodNames = [1]string{"scan"}

// A potential covered call candidate found by the scanning engine.
type Candidate struct {
	// Stock Ticker Symbol
	Ticker string
	// Current underlying stock price
	StockPrice float64
	// Option Strike Price
	StrikePrice float64
	// Expiration Date (YYYY-MM-DD)
	ExpirationDate string
	// Option Premium Price
	Premium float64
	// Calculated annualized return yield %
	AnnualizedYield float64
}

// ScanPayload is the payload type of the scanner service scan method.
type ScanPayload struct {
	// List of tickers to scan
	Tickers []string
	// Minimum annual return yield %
	MinAnnualizedYield *float64
}
